// const fs = require('fs');
// const path = require('path');

// fs.readdirSync(__dirname).filter(file => (file.indexOf('.') !== 0) && (file !== path.basename(__filename)) && (file.slice(-3) === '.js')) .forEach((file) => {
//     console.log("ok")
// });


// return  require('./newsService');